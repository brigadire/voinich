# Generic structural analyzer

Implementation: `internal/notation/analyze.go`; CLI:
`go run ./cmd/notation-corpus analyze --fixture --input X --output Y`.
The preparation lock rejects non-fixture analysis. It contains no corpus, VM,
music, Latin, or cipher branches.
