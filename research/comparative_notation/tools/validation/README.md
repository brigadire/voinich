# Validation

Schema, hierarchy, and ordering checks are in `internal/notation/types.go`;
class-dimensional checks are in `adapter.go`; unit and metamorphic tests are
in `internal/notation/*_test.go`. Only fixtures/synthetic data are exercised.
