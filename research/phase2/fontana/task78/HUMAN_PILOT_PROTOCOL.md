# F12 human pilot protocol (not executed)

No participant was available within task78, so this file preregisters a future
pilot and records no results. Machine trials must not be relabeled as human
recall.

For each consenting participant, train 12 arbitrary cue–intention pairs, keep
4 unseen foils, and test after 10 minutes and 24 hours in randomized order:
intact correct-state cue, no device/cue, one damaged cue, and a valid cue from
the wrong state. Record exact intended-item recall, false recall, latency and
confidence. An untrained control sees the same states without the association
list. The cue labels, allocation seed, exclusion rule (technical failure only)
and primary endpoint (exact recall at 24 hours) must be frozen before training.

Report participant-level results and Wilson intervals but make no population
claim from a small convenience sample. Semantic guesses, partial recall and
formal cue lookup remain separate outcomes. Until this protocol is actually
run, `HUMAN_MNEMONIC_EFFECT = INCONCLUSIVE`.
