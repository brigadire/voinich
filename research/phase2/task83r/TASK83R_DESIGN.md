# Task83r repeated confirmatory design

Status: **FROZEN BEFORE TARGET OPENING**.

Task83r is a new execution of the unchanged scientific protocol in
`research/phase2/task83/TASK83_DESIGN.md` and the unchanged comparison contract
in `research/phase2/task82a1/TASK83_COMPARISON_CONTRACT.md`. Its only target is
the authoritative Fingerprint V2.1 deterministic refreeze issued by Task83b.
The invalid Task83 comparison outputs are quarantined and are neither inputs
nor interpretive priors.

The primary space is the frozen direct F2 intersection. Projection metrics are
secondary and are never combined with direct distances. Both ZL3b and IT2a are
mandatory. Frozen natural controls, simple nulls, every Fontana mechanism,
Task82b shorthand, extraction operators, and their matched nulls are retained
without selection or retuning. Missing values are not imputed. Metric
distances are normalized only by pre-target natural-control scales, averaged
within frozen families, and family-balanced. Endpoint and trajectory evidence
remain separate; compatible is not synonymous with closest.

The endpoint envelope, trajectory statistics, coverage penalty, overshoot
rule, null-separation rule, evidence levels, pairwise-advantage rule, and
equifinality/identifiability gates are exactly those frozen by Task83. AX is
descriptive only. FIRST/LAST extraction retains the line-collapse confound.
Shorthand conclusions are limited to the tested BDD tradition. No post-opening
change to metrics, parameters, seeds, weights, normalization, nulls, coverage,
aggregation, corpora, mechanisms, or decision thresholds is permitted.
