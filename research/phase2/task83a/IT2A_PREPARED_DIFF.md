# IT2a prepared-corpus difference audit

Only one candidate has recoverable bytes. The `10286e…` corpus is 229,490 bytes, 5,207 LF-terminated lines, and 37,945 whitespace tokens. It is byte-identical to two clean current regenerations and one Task79c-source regeneration.

No file or Git object with SHA-256 `3fb953…` was found. Consequently its size, first differing byte, changed lines/loci, order, whitespace/newline behavior, and transcription content are **NOT AVAILABLE**. Fabricating variants or searching preprocessing switches to hit the desired hash is explicitly forbidden. A byte-level or scientific delta between unavailable bytes and `10286e…` therefore cannot honestly be computed.

This absence does not make `3fb953…` a competing scientific corpus: it appears only as a later top-level metadata literal, while the prepared sidecar and independently embedded Task79c artifacts all bind `10286e…`.
