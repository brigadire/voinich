# Task85b — G1-v2 preregistered design

## Scope and historical boundary

This task freezes a measurement and execution contract; it executes neither the new blind controls nor Voynich. Historical results are preserved exactly: Task86R reported `G1_MINIMAL_CLASS=NONE`, qualified by `TASK86R_CONFIRMATORY_INTEGRITY_QUALIFIED`; Task86C recovered 0 synthetic M0–M5 classes, returned NONE for natural controls, `G1_MEASUREMENT_IDENTIFIABLE=NOT_SUPPORTED`, `TASK86R_NONE_INTERPRETABILITY=NOT_INTERPRETABLE`, and `G1_V2_REQUIRED=YES`; Task86C-a returned `TASK86C_A_EVIDENCE_INSUFFICIENT`. Task86C-a does not establish PM6 as the sole cause.

## Scientific contract

The primary question is the minimum tested class sufficient for token formation under a validated identifiable protocol. M0–M5 retain their conceptual ladder. Changes and their scientific classification are explicit in `G1V1_G1V2_CHANGE_REGISTER.tsv`; implementation failure never rejects a model class.

Predictive evidence follows `PM_REDESIGN.md` and `PM6_NEGATIVE_PROTOCOL.md`. Required metrics use PASS/FAIL/NOT_ASSESSABLE. PM1 is retained diagnostic evidence, PM2/PM5/PM6 are gates, and PM4 is supporting evidence. Thresholds follow frozen development derivations, never Task86C confirmatory outcomes. Structural evidence follows `STRUCTURAL_ADEQUACY_V2.md`; skeleton-only metrics cannot count. Minimality follows `MINIMALITY_AND_IDENTIFIABILITY.md`, including equivalence sets and NOT_IDENTIFIABLE.

Every candidate terminates with separate computational, induction, metric, generation, structural, and model statuses. `MODEL_INADEQUATE` requires valid negative evidence. Training/induction/convergence/generation failures and missing metrics produce `MODEL_NOT_IDENTIFIABLE`. NONE is legal only after complete valid rejection; otherwise final output is NOT_IDENTIFIABLE.

## Evidence and execution

The evidence contract makes every decision reconstructible without fitting or generation. Workers emit immutable facts; the decision verifier alone applies frozen gates. The Phase-I mTLS lease executor is reused through a G1-v2 adapter, with an explicit DAG, deterministic scientific JobID, content hashes, resume, dynamic N-worker claiming, infrastructure retries, duplicate quarantine, and deterministic aggregation. Executor policy contains no scientific reachability logic.

## Validation and firewalls

Open development controls may validate engineering and derive predeclared thresholds. New confirmatory M0–M5 generators are independent, opaque, and escrowed until analysis freeze. Natural-language sources use fixed language-neutral tokenization and matched sampling. Task86C controls diagnose defects but never validate G1-v2 or tune thresholds. Voynich data cannot be opened by Task85b/Task86C-v2 or influence any definition, threshold, induction choice, or recovery criterion.

Task86C-v2 must pass model recovery, ordinary-language applicability, evidence-chain reconstruction, cross-node determinism, distributed verification, failure recovery, and scaling before Task86V is eligible. Task87 remains blocked.
