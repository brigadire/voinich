# Task85c-a report

## Outcome

The original `A17_STATUS_SCHEMA_CONTRADICTION` remains unresolved because exhaustive audit expanded it to a normative status/reachability contradiction. Findings E01–E04 are independently reproduced by `reference/reproduce_expanded_defect.py`.

No `G1_V2_EXECUTABLE_CONTRACT_V1_1` was issued. The V1 parent and the scientific-impl-R failure closure remain byte-unchanged. No models, RNG, candidates, metrics, thresholds, controls, corpora, DAG, production code, blind inputs, natural confirmatory results, or Voynich inputs were changed or executed.

## Verdicts

`PARENT_TASK85C_IDENTITY = SUPPORTED` and `A17_DEFECT_REPRODUCED = SUPPORTED`.

`EVIDENCE_STATUS_MATRIX_COMPLETE`, `STANDARD_SCHEMA_STATUS_ENFORCEMENT`, `STATUS_DEPENDENT_FIELD_ENFORCEMENT`, `ORIGINAL_DEFECT_REGRESSION`, `HASH_VALID_INVALID_EVIDENCE_REJECTED`, `SCIENTIFIC_SEMANTICS_UNCHANGED`, `GOLDEN_SUITE_EXTENDED`, `TRANSITIVE_REFREEZE`, and `TASK86C_V2_SCIENTIFIC_IMPL_R2_READY` are `NOT_SUPPORTED` because no unique frozen matrix exists.

`CANONICALIZATION_UNCHANGED`, `REACHABILITY_UNCHANGED`, `DAG_UNCHANGED`, `MODEL_SPACE_UNCHANGED`, `RNG_UNCHANGED`, `CONTROL_CORPUS_DESIGN_UNCHANGED`, `BLIND_FIREWALL_PRESERVED`, `NATURAL_CONFIRMATORY_FIREWALL_PRESERVED`, and `VOYNICH_FIREWALL_PRESERVED` are `SUPPORTED` in the limited sense that this stopped task made no changes or prohibited executions. This does not validate the internally inconsistent reachability semantics.

`TERMINAL_MARKER = TASK85C_A_CONTRACT_DEFECT_EXPANDED`.

