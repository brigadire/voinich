# Task85c-f report

PF-SC01 is reproducible, and the proposed conditional-admissible-support rule
closes that boundary in isolation. Two independently written reference paths
agree on all eight categorical boundary vectors and on the PF-SC01 first glyph
`d`, consuming one draw.

The full audit nevertheless requires the mandated expanded-defect stop. The
Generator B registry is not an executable sampling specification: it names
exponential races, Walker alias, and cumulative matrix rows without defining
the required outcome/derivation enumeration, alias tables, rounding, U53
allocation, `u=0`, or tie behavior. The only builder provenance hashes those
descriptive strings; it supplies no missing module or tables. This can change
bytes even when EOS is not token-initial.

Four further independent gaps prevent the required unique corpus function:

1. “UTF-8-sorted outcomes” conflicts with the explicit M0 registry order used
   by the reproduced fixture (literal `<EOS>` sorts before `a`, but the row
   places EOS last).
2. The check point for “length 64 without EOS” is not reconciled with M5's
   support through 64 glyphs.
3. M5's 1024 successive attempts do not allocate/reset counters and component
   draws uniquely.
4. The token abstraction does not freeze delimiter and final-newline bytes for
   serialized generated corpora.

Completing these choices would revise algorithms and corpus bytes beyond the
narrow PF-SC01 boundary. Consequently PF-SC01 cannot be marked CLOSED in an
authoritative V1.2, and no files named
`G1_V2_EXECUTABLE_CONTRACT_V1_2.{md,json}` or V1.2 golden suite are created.
The candidate state machine and vectors are explicitly non-normative evidence.

Parent V1.1, E1, status/reachability V2, G1V2-RNG-1, candidate/metric/decision
registries, and R2-G01/R2-G02/EI01 remain unchanged. The scientific firewall is
intact; production-freeze retry is not ready.
