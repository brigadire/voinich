# Capacity report

`CAPACITY_QUALIFIED = NOT_SUPPORTED`. Production handlers cannot be benchmarked until PF-SC01 is repaired and conforming implementations exist. This does not authorize changing the frozen workload.
