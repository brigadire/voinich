# Blocked runbook

`PRODUCTION_RUN_AUTHORIZED=NO`. Coordinator submission and worker deployment
are prohibited. The current engineering executable must not be used with a
production manifest. A future retry must verify V1.2 identities and generation
closure, obtain `SCIENTIFIC_IMPLEMENTATION_COVERAGE=COMPLETE`, qualify actual
handlers, and only then materialize secrets, controls, JobIDs and the DAG.
