# Reproducibility report

The section lexical pipeline was executed twice with identical frozen inputs, deterministic ordering, seed `20260831`, thresholds, and page/leaf grouping. All 15 non-manifest TSV/Markdown outputs were byte-identical between runs. No visual schema or textual fingerprint input changed.

`SECTION_LEXICON_REPRODUCIBLE=true`
