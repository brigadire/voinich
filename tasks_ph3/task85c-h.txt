[O[ITask85c-h — G1-v2 V1.2 Production Scientific Implementation,
Handler Closure and Pre-Materialization Validation


PURPOSE

Implement the complete production scientific execution layer required
to execute the already frozen G1-v2 V1.2 experiment.

This task begins only after successful completion of:

    Task85c-i

which closed:

    H-SC01-EVIDENCE-CONTRACT-VERSION
    H-SC02-E1-JOBID-SCIENTIFIC-VERSION

and froze a coherent V1.2 authority closure consisting of:

    G1_V2_EXECUTABLE_CONTRACT_V1_2
    G1V2_V1_2_INTEGRATION_SUPPLEMENT_I1
    G1V2_EXECUTION_IDENTITY_ERRATUM_E2
    G1V2_EVIDENCE_CONTRACT_V1_2
    G1_V2_STATUS_REACHABILITY_CONTRACT_V2
    G1V2_GENERATION_SEMANTICS_V1.

Task85c-h MUST implement the scientific handlers and execution
semantics already frozen by these authorities.

Task85c-h MUST NOT redesign, tune, extend, simplify or reinterpret the
scientific experiment.

The task ends BEFORE production materialization.

It MUST NOT create:

    production escrow key;
    production blind synthetic controls;
    production natural confirmatory controls;
    production blind IDs;
    production thresholds;
    production JobIDs;
    production DAG;
    production run manifest.

It MUST NOT execute:

    blind confirmatory recovery;
    natural-language confirmatory recovery;
    Voynich evaluation;
    production experiment.

The successful output of Task85c-h is a complete, deterministic,
scientifically traceable and independently validated implementation
ready for:

    Task86C-v2-production-freeze.


======================================================================
1. CURRENT SCIENTIFIC AUTHORITY
======================================================================

Scientific contract:

    G1_V2_EXECUTABLE_CONTRACT_V1_2

Markdown SHA-256:

    ec60bb23e55ce157fe954b5cafc63d22ab70ecec390822cb63f9ae273142c639

Machine JSON SHA-256:

    29e39e0c25dc8033f784480fdc537e3ede9eeb69baa0607c9f249d796d6b42dc


======================================================================
2. V1.2 INTEGRATION AUTHORITY
======================================================================

Integration authority:

    G1V2_V1_2_INTEGRATION_SUPPLEMENT_I1

Machine SHA-256:

    35ecf0bfc9a9c27bb63d33b074bc399dd9256692620ee06a54e592e0d1e867b2

Markdown SHA-256:

    9953925f894ea070e65028b89b3204039d5bc9d34a6c09fa67e7d0367585b2ff

For future V1.2 execution I1 binds:

    scientific contract identity
        =
    E2/JobID scientific identity
        =
    evidence contract identity
        =
    evidence schema-family binding
        =
    G1_V2_EXECUTABLE_CONTRACT_V1_2.

Historical V1.1 evidence schemas and E1 remain provenance only and
MUST NOT override I1 for V1.2 execution.


======================================================================
3. EXECUTION IDENTITY AUTHORITY
======================================================================

Execution identity:

    G1V2_EXECUTION_IDENTITY_ERRATUM_E2

Machine SHA-256:

    cfaa8c1cb787380baca5a391d077e5d2b855df8bba767ad5b601457e06eb0070

Markdown SHA-256:

    9b5999a55bf536620125ff481ccbd5a10bb16f63d90a402a2ff375f02b11852d

Required scientific identity:

    scientific_contract_version =
        G1_V2_EXECUTABLE_CONTRACT_V1_2

    jobid.scientific_identity_version =
        G1_V2_EXECUTABLE_CONTRACT_V1_2.

E2 preserves the E1 boundary:

    SCIENTIFIC_RANDOMNESS_SOURCE =
        G1V2-RNG-1

    BLIND_ID_ROLE =
        OPAQUE_EXECUTION_IDENTIFIER

    ESCROW_ROLE =
        BLINDNESS_AND_TRUTH_COMMITMENT

    JOBID_ROLE =
        RUN_EXECUTION_IDENTIFIER_BOUND_TO_SCIENTIFIC_JOB

    SCIENTIFIC_IDENTITY_DEPENDS_ON_ESCROW_KEY =
        NO

    SCIENTIFIC_IDENTITY_DEPENDS_ON_BLIND_ID_BYTES =
        NO.


======================================================================
4. EVIDENCE AUTHORITY
======================================================================

Evidence contract:

    G1V2_EVIDENCE_CONTRACT_V1_2

V1.2 evidence schema count:

    15

V1.2 evidence schema root:

    39c4c3bee96ee58ddd38552cbb16fdc2f994a390b86a6645e1420c3cb67eca81

V1.2 evidence schema registry SHA-256:

    b89f3e2e0a0d30d8526951717c03527b845687ceebd3f7741c762d0503fe8ca9

All production scientific evidence generated by the implementation
must validate against this V1.2 evidence family.

Unknown, mixed or V1.1 scientific identities MUST fail closed.


======================================================================
5. STATUS/REACHABILITY AUTHORITY
======================================================================

Status/reachability authority:

    G1_V2_STATUS_REACHABILITY_CONTRACT_V2

SHA-256:

    fc1ca07d8123ed5d44bc24ecba98fca54d5b05781ecbaba820d44079319038b9

Task85c-h MUST implement exactly the frozen:

    13 statuses;
    7 DAG stages;
    45 direct transitions;
    suppression semantics;
    multi-dependency precedence;
    aggregation semantics.

Central invariant:

    execution/procedure failure != scientific FAIL.

PASS/FAIL are reserved for assessable scientific gate/verifier
outcomes exactly as frozen.


======================================================================
6. GENERATION AUTHORITY
======================================================================

Generation semantics:

    G1V2_GENERATION_SEMANTICS_V1

SHA-256:

    45d533f8b83b24c77a96836fa5c2ef95f9b948003bd2ed725fc2ea97e010b310

Generation state-machine SHA-256:

    aea10f4b117488e5ed60e259b392d32248ba0c18de89e91dd3f9f62b7add406a

Generation golden root:

    143954667073a2c10f1bd59ce98b9c93dd84b50632bb67ea80d0d92449480acb

Generation serialization SHA-256:

    c1aaf434b8bd3e5c4284d9a98ee86820b863c98331d03936e40efb6bf2b23974

Required:

    generation paths = 26/26
    PF_SC01 = CLOSED.

Task85c-h MUST NOT introduce any new generation choice.


======================================================================
7. TASK85C-I AUTHORITY
======================================================================

Task85c-i artifact root:

    4dca7e3bf3d28ad1852ac0ebfee3ae6f4e4410227a1e57d491587874a126f84c

Task85c-i results manifest SHA-256:

    fdfbcdfdb54f8eed538185a612f9e26aaab2e634d64674526cb4b7734979bcba

Task85c-i terminal marker:

    G1V2_V1_2_EVIDENCE_EXECUTION_INTEGRATION_I1_FROZEN

Required parent verdict:

    TASK85C_H_RETRY_READY =
        SUPPORTED.


======================================================================
8. HISTORICAL FAILED TASK85C-H
======================================================================

The previous Task85c-h attempt is historical provenance.

It terminated before scientific implementation because the then-current
authority closure contained H-SC01 and H-SC02.

Do NOT modify or overwrite its artifacts.

If repository layout permits, preserve it under a clearly historical
location such as:

    research/phase3/task85c-h-v1_2-pre-i1-failure/

or retain its existing immutable location.

The current Task85c-h is a clean retry under I1/E2.


======================================================================
9. PRE-IMPLEMENTATION AUTHORITY GATE
======================================================================

Before writing production scientific handlers verify all authoritative
hashes listed above.

Require:

    V1.2 scientific contract = SUPPORTED
    I1 = SUPPORTED
    E2 = SUPPORTED
    V1.2 evidence root = SUPPORTED
    status/reachability V2 = SUPPORTED
    generation semantics = SUPPORTED
    generation goldens = SUPPORTED
    Task85c-i root = SUPPORTED
    Task85c-i results manifest = SUPPORTED.

Also require:

    H_SC01 = CLOSED
    H_SC02 = CLOSED
    R2_G01 = CLOSED
    R2_G02 = CLOSED
    EI01 = CLOSED
    PF_SC01 = CLOSED
    SCIENTIFIC_SEMANTIC_CHANGE = 0
    SCIENTIFIC_FIREWALL = INTACT.

If this gate fails:

    STOP TASK85C_H_AUTHORITY_GATE_FAILED.


======================================================================
10. AUTHORITY PRECEDENCE
======================================================================

Use the frozen V1.2 precedence.

For scientific semantics:

    G1_V2_EXECUTABLE_CONTRACT_V1_2.json
        >
    normative V1.2 scientific registries
        >
    G1V2_GENERATION_SEMANTICS_V1
        >
    generation goldens
        >
    explanatory Markdown.

For cross-artifact V1.2 integration:

    G1V2_V1_2_INTEGRATION_SUPPLEMENT_I1.

For execution identity:

    G1V2_EXECUTION_IDENTITY_ERRATUM_E2.

For evidence validation:

    G1V2_EVIDENCE_CONTRACT_V1_2
    and its frozen schema registry/root.

For status/reachability:

    G1_V2_STATUS_REACHABILITY_CONTRACT_V2.

Do not resolve a contradiction by choosing lower-precedence prose.


======================================================================
11. IMPLEMENTATION PRINCIPLE
======================================================================

The implementation must be a translation of frozen scientific
semantics into executable code.

For every scientific operation, there must be a traceable answer to:

    Which normative artifact defines this operation?

    Which machine-readable field or frozen rule defines its
    parameters?

    Which implementation function executes it?

    Which test proves conformance?

    Which evidence field records its result?

No scientific behavior may exist solely because it appeared
reasonable to the implementer.


======================================================================
12. NO IMPLEMENTER SCIENTIFIC CHOICE
======================================================================

A second independent implementer given the frozen authorities and the
same input must have no permitted scientific choice capable of
changing:

    fitted model;
    generated corpus;
    RNG consumption;
    PM result;
    F2 result;
    gate result;
    complexity;
    equivalence;
    minimality;
    final verdict.

Required:

    SECOND_IMPLEMENTER_UNSTATED_CHOICES = 0.


======================================================================
13. REQUIRED CANDIDATE COVERAGE
======================================================================

Implement all:

    43 frozen candidate specifications

across:

    M0
    M1
    M2
    M3
    M4
    M5.

No candidate may remain:

    placeholder;
    TODO;
    stub;
    transport-only;
    mock;
    unsupported.

Require:

    CANDIDATES_IMPLEMENTED = 43/43.


======================================================================
14. M0 IMPLEMENTATION
======================================================================

Implement frozen M0 exactly:

    glyph IID;
    frozen vocabulary semantics;
    additive-alpha probability;
    frozen boundary treatment;
    frozen fitting;
    frozen prediction;
    frozen generation;
    frozen complexity accounting.

No smoothing or vocabulary choice may be invented.


======================================================================
15. M1 IMPLEMENTATION
======================================================================

Implement frozen M1 exactly:

    orders 1–3;
    suffix context;
    frozen smoothing;
    frozen suffix backoff;
    frozen boundary handling;
    unseen-event behavior;
    candidate selection;
    deterministic tie handling;
    generation;
    complexity accounting.


======================================================================
16. M2 IMPLEMENTATION
======================================================================

Implement frozen M2 PST semantics exactly:

    frozen context construction;
    likelihood-gain criterion;
    depth/grid;
    pruning;
    escape/backoff;
    stopping;
    tie rules;
    generation;
    complexity accounting.


======================================================================
17. M3 IMPLEMENTATION
======================================================================

Implement both frozen M3 routes:

    exact route;
    approximate route.

Exact route:

    deterministic probabilistic DFA;
    frozen exhaustive-search semantics;
    frozen ordering;
    frozen bounds;
    frozen score;
    frozen divergence;
    frozen accounting.

Approximate route:

    frozen deterministic JS-merge semantics;
    frozen candidate ordering;
    frozen merge rules;
    frozen search bound;
    frozen tie handling;
    frozen accounting.

Required frozen bounds include:

    exact route <= 1e8
    approximate route <= 1e7

where specified by the contract.

No route may silently substitute for the other.


======================================================================
18. M4 IMPLEMENTATION
======================================================================

Implement frozen first-order discrete HMM:

    frozen state grid;
    initialization;
    Baum-Welch;
    smoothing;
    8 restarts;
    500-iteration cap;
    frozen RNG derivation;
    objective;
    tolerance;
    stopping;
    tie handling;
    generation;
    complexity accounting.

Numerical failure must map to the frozen procedural status and never
be silently converted into scientific FAIL.


======================================================================
19. M5 IMPLEMENTATION
======================================================================

Implement frozen productive:

    prefix / stem / suffix

grammar exactly.

Include:

    frozen component inventories;
    frozen supports;
    frozen mixtures;
    backoff;
    weights;
    candidate grid;
    unseen behavior;
    generation;
    complexity.

Generation attempts are exactly:

    0..1023.

Consumed RNG draws persist across attempts exactly as frozen.

Attempt exhaustion:

    GENERATION_FAILURE.

No hidden retry/reset policy is permitted.


======================================================================
20. TOKEN/VOCABULARY SEMANTICS
======================================================================

Implement frozen corpus abstraction:

    ordered nonempty NFC tokens.

Vocabulary:

    DEVELOPMENT glyphs;
    deterministic NFC UTF-8 ordering as frozen;
    UNK;
    EOS;
    BOS as context-only where specified.

Non-DEVELOPMENT glyphs map to:

    UNK

exactly as frozen.


======================================================================
21. CORPUS SPLIT
======================================================================

Implement the frozen occurrence split:

    DEVELOPMENT = 60%
    VALIDATION  = 20%
    HELDOUT     = 20%.

Fit only on:

    DEVELOPMENT.

Select using:

    VALIDATION.

Do not refit after selection.

Evaluate final predictive behavior on:

    HELDOUT.


======================================================================
22. CANDIDATE SELECTION
======================================================================

Implement frozen candidate selection:

    smallest VALIDATION PM2;
    then complexity;
    then candidate ID

using the exact frozen tie rules.

No post-hoc selection criterion is permitted.


======================================================================
23. NUMERICAL SEMANTICS
======================================================================

Implement frozen numerical semantics including:

    IEEE-754 binary64;
    round-to-nearest ties-to-even;
    base-2 logarithms;
    Neumaier summation;
    frozen ascending scientific order;
    type-7 quantiles;
    canonical decimal evidence representation.

Do not replace frozen summation/order semantics with language/library
defaults when those defaults differ.


======================================================================
24. RNG
======================================================================

Implement:

    G1V2-RNG-1

exactly.

Root RNG seed:

    6f5a9c731de248b480c66b237ace215044689c5fa2f593e510b73dce18a49027

Implement frozen:

    domain separation;
    derivation;
    PRNG;
    U53 conversion;
    draw indexing;
    consumption;
    golden vectors.

Every U53 increments exactly one corpus-global draw index where frozen
generation semantics require it.


======================================================================
25. GENERATOR A
======================================================================

Implement Generator A exactly as V1.2 freezes it.

Required:

    direct inverse-CDF;
    frozen admissible-support filtering;
    frozen renormalization;
    frozen outcome order;
    exact RNG consumption.


======================================================================
26. GENERATOR B
======================================================================

Implement Generator B independently from Generator A.

Required frozen mechanisms include:

    M0/M2/M3/M5:
        fully ordered exponential race

    M1:
        deterministic Walker alias table

    M4:
        materialized cumulative matrix rows.

Generator B MUST NOT call Generator A internally.

Scientific equivalence must arise from the frozen construction, not
shared implementation code that bypasses the independence requirement.


======================================================================
27. NONEMPTY TOKEN GENERATION
======================================================================

Implement V1.2 admissible-support semantics exactly.

Permanent PF-SC01 regression:

    first frozen M0_GEN_A draw:
        U53 = 0.92848667210989588

must produce:

    emitted first glyph = d
    RNG draws consumed = 1
    next state = INSIDE_TOKEN.

Require:

    PF_SC01 = CLOSED.


======================================================================
28. TOKEN LENGTH
======================================================================

Length counts emitted Unicode scalar glyphs.

Length:

    64

is legal.

After glyph 64, perform frozen structural completion without consuming
an additional random draw where specified.

No implementation may reinterpret 64 as an invalid overlength token.


======================================================================
29. CORPUS SERIALIZATION
======================================================================

Implement canonical generated corpus bytes exactly:

    NFC UTF-8;
    one token per line;
    LF after every token;
    including the final token.

Empty logical corpus:

    zero bytes

but is invalid where a requested synthetic control requires nonempty
content.

Serialization must be covered by byte-level goldens.


======================================================================
30. FIT STAGE
======================================================================

Implement the production scientific:

    FIT

stage for every applicable candidate.

FIT output must include exactly the evidence required by the frozen
V1.2 evidence architecture.

Successful fit:

    FIT_SUCCESS.

Procedural failures use only frozen procedural statuses such as:

    FIT_FAILURE
    NUMERICAL_FAILURE
    INDUCTION_CAP
    PROTOCOL_VETO

where applicable.

Never emit scientific FAIL merely because fitting failed.


======================================================================
31. FITTED MODEL SERIALIZATION
======================================================================

Implement deterministic fitted-model serialization.

Equivalent execution on the same input must produce byte-identical
model artifacts.

Serialization must contain all scientific state required downstream
and no machine-local execution state.


======================================================================
32. PREDICTIVE STAGE
======================================================================

Implement the complete frozen predictive evaluation including:

    PM1
    PM2
    PM4
    PM5
    PM6

where required by the candidate/protocol.


======================================================================
33. PM1
======================================================================

Implement frozen held-out description/predictive quantity exactly as
specified.

It must integrate with:

    description length =
        model complexity + HELDOUT PM1

where frozen minimality logic requires it.


======================================================================
34. PM2
======================================================================

Implement frozen PM2 including:

    2000 DEVELOPMENT paired bootstrap replicates;
    frozen one-sided p-value;
    Holm correction at alpha = 0.05;
    frozen practical/effect floor;
    frozen q95 comparison;
    deterministic RNG derivation;
    deterministic ordering.

No library-default bootstrap or multiplicity semantics may replace
the frozen contract.


======================================================================
35. PM5
======================================================================

Implement exact frozen adaptive-bin ECE.

Include all frozen:

    bin construction;
    edge handling;
    weighting;
    empty-bin behavior;
    calibration statistic;
    resampling.

PM5 uses the frozen paired bootstrap/gate semantics.


======================================================================
36. PM6
======================================================================

Implement frozen discrimination test:

    finite complement;
    one negative per positive;
    coverage >= 80%;
    >= 100 pairs;
    >= 2 lengths;
    2000 bootstrap/permutation replicates.

PASS requires exactly the frozen conditions including:

    bootstrap LCB > 0.5
    observed AUC > permutation q95.

Saturation:

    NOT_ASSESSABLE

where frozen.

Do not reinterpret saturation as FAIL.


======================================================================
37. PREDICTIVE GATE
======================================================================

Implement frozen predictive gate.

Predictive PASS requires the exact frozen combination of:

    PM2
    PM5
    PM6.

No implementation-specific fallback is permitted.


======================================================================
38. GENERATION STAGE
======================================================================

Implement production GENERATION stage using the complete frozen V1.2
generation semantics.

Required generation scales:

    2k
    8k
    32k

and:

    4 replicates

where defined.

Generation failures must emit frozen procedural evidence and suppress
dependent F2 jobs exactly according to V2 reachability.


======================================================================
39. F2 METRIC STAGE
======================================================================

Implement all:

    12 frozen F2 metrics.

Require:

    F2_METRICS_IMPLEMENTED = 12/12.


======================================================================
40. F2 METRIC INVENTORY
======================================================================

The frozen structural metrics include the specified metrics in:

    EF1
    EF2
    EF3
    LP1
    LP4
    HR1
    LS1
    PF5

with all contract-defined submetrics producing the complete set of 12.

Examples include:

    giant component;
    isolate behavior;
    clustering;
    degree-frequency Spearman;
    support Gini;
    prefix/suffix NMI;
    folio/locus/section variance;
    line-length CV;
    within-folio progression.

Use the exact frozen definitions and thresholds.

Do not infer missing formulas from metric names.


======================================================================
41. SKELETON DIAGNOSTICS
======================================================================

Implement SKELETON diagnostics where required.

Their frozen scientific weight is:

    zero.

They must never alter adequacy or final verdict.


======================================================================
42. STRUCTURAL FAMILIES
======================================================================

Implement frozen structural family evaluation including:

    EDIT
    LEXICAL_PARADIGM

and all other family organization frozen by V1.2.

Both required families must satisfy all required scales for structural
PASS exactly as frozen.


======================================================================
43. STRUCTURAL GATE
======================================================================

Implement structural:

    metric;
    family;
    verdict;
    gate

semantics exactly.

Do not introduce new aggregation or tolerance rules.


======================================================================
44. COMPLEXITY STAGE
======================================================================

Implement exact frozen canonical bit-cost accounting.

For every candidate, complexity must be reproducible from frozen model
serialization and registry rules.

No host-dependent object-size or serializer-size proxy is permitted.


======================================================================
45. DESCRIPTION LENGTH
======================================================================

Implement:

    description length =
        complexity
        +
        HELDOUT PM1

exactly where frozen.


======================================================================
46. EQUIVALENCE
======================================================================

Implement frozen equivalence edge criterion:

    abs(delta)
        <=
    max(
        1,
        DEVELOPMENT q95 paired delta
    )

with exact frozen numerical semantics.


======================================================================
47. MINIMALITY
======================================================================

Implement frozen minimality graph/component logic.

The final minimal class logic must distinguish:

    singleton recovered class;
    multi-class NOT_IDENTIFIABLE;
    NONE;
    missing-evidence NOT_IDENTIFIABLE

exactly as frozen.


======================================================================
48. ADEQUACY
======================================================================

A candidate is adequate only when all frozen requirements hold,
including:

    successful fit;
    required generation;
    predictive PASS;
    structural PASS;
    required hashes/evidence integrity.

No procedural failure may be converted into scientific inadequacy
unless the frozen decision rules explicitly permit it.


======================================================================
49. FINAL VERDICT
======================================================================

Implement the frozen final verdict domain:

    RECOVERED_M0
    RECOVERED_M1
    RECOVERED_M2
    RECOVERED_M3
    RECOVERED_M4
    RECOVERED_M5
    NONE
    NOT_IDENTIFIABLE
    PROTOCOL_INVALID.

Require exact frozen semantics.

In particular:

    NONE

is legal only when every relevant candidate completed validly and was
assessably inadequate according to the frozen protocol.

Missing evidence capable of changing the minimum:

    NOT_IDENTIFIABLE

unless a protocol-invalid condition is frozen to take precedence.


======================================================================
50. STATUS IMPLEMENTATION
======================================================================

Implement all frozen statuses exactly:

    PASS
    FAIL
    NOT_ASSESSABLE
    FIT_SUCCESS
    GENERATION_SUCCESS
    COMPLEXITY_SUCCESS
    AGGREGATION_SUCCESS
    FIT_FAILURE
    NUMERICAL_FAILURE
    INDUCTION_CAP
    GENERATION_FAILURE
    PROTOCOL_VETO
    NOT_REACHED.

Do not recreate:

    SCIENTIFIC_FAILURE

as a status.

`scientific_failure` may exist only as the frozen evidence type
carrying concrete procedural failure statuses.


======================================================================
51. DAG STAGE MODEL
======================================================================

Implement exactly the seven frozen production DAG stages:

    FIT
    PREDICTIVE
    GENERATION
    F2_METRIC
    COMPLEXITY
    CANDIDATE_AGGREGATION
    CONTROL_AGGREGATION.

Do NOT create STRUCTURAL as an eighth DAG stage.

Structural family/gate evaluation is a frozen suboperation of candidate
aggregation.


======================================================================
52. REACHABILITY
======================================================================

Implement frozen reachability exactly.

All jobs are conceptually planned.

A downstream job runs only when all required upstream transitions
resolve to:

    RUN.

Suppressed jobs produce:

    NOT_REACHED

evidence as frozen.


======================================================================
53. MULTI-DEPENDENCY SUPPRESSION
======================================================================

Implement frozen suppression precedence:

    PROTOCOL_VETO
    procedure failure
    NOT_ASSESSABLE
    FAIL
    NOT_REACHED

in the exact normative ordering.

Where multiple upstream dependencies suppress the same job:

    choose the decisive upstream according to frozen precedence;
    tie-break using lowest bytewise upstream JobID;
    retain all dependency identities.

Do not depend on scheduler arrival order.


======================================================================
54. AGGREGATION
======================================================================

Implement:

    CANDIDATE_AGGREGATION
    CONTROL_AGGREGATION

exactly.

Aggregation jobs always execute as frozen and summarize upstream
status/evidence rather than disappearing because a scientific gate
failed.


======================================================================
55. JOBID SUPPORT
======================================================================

Implement E2-compatible JobID construction for disposable fixtures.

Required form:

    "j-"
        +
    first 40 lowercase hexadecimal characters
    of SHA-256 over the frozen canonical construction.

Use:

    dependency_job_ids

as the canonical dependency field.

Do not create production JobIDs in this task.


======================================================================
56. JOBID EXCLUSIONS
======================================================================

Scientific JobID payload MUST exclude:

    hostname;
    worker;
    coordinator;
    lease;
    retry;
    scheduling order;
    timestamp;
    wall-clock time.

Preserve E2 exactly.


======================================================================
57. EVIDENCE PRODUCTION
======================================================================

Every scientific handler must emit V1.2 evidence conforming to:

    G1V2_EVIDENCE_CONTRACT_V1_2.

All emitted evidence must use:

    contract_version =
        G1_V2_EXECUTABLE_CONTRACT_V1_2.

No V1.1 evidence may be emitted by a V1.2 scientific handler.


======================================================================
58. REQUIRED EVIDENCE TYPES
======================================================================

Implementation must support all 15 frozen V1.2 evidence types:

    complexity
    f2_metric
    final_verdict
    fit
    fitted_model
    generation
    minimality
    not_reached
    predictive_gate
    predictive_metric
    predictive_verdict
    scientific_failure
    structural_family
    structural_gate
    structural_verdict.

Require:

    EVIDENCE_TYPES_IMPLEMENTED = 15/15.


======================================================================
59. EVIDENCE VALIDATION
======================================================================

Validate handler-produced disposable evidence against the frozen
V1.2 schemas.

Require:

    valid handler evidence → ACCEPT

and appropriate mutations:

    wrong contract version;
    wrong schema ID;
    illegal status;
    missing required field;
    mixed identity

→ REJECT.


======================================================================
60. EVIDENCE-ONLY VERIFIER
======================================================================

Provide/update an evidence-only verifier that can reconstruct:

    predictive gates;
    structural gates;
    adequacy;
    complexity/minimality decisions;
    final verdict

from evidence alone where required by the frozen architecture.

The verifier MUST NOT import fitting or generation implementations.


======================================================================
61. VERIFIER INDEPENDENCE
======================================================================

The evidence-only verifier must not merely call the same high-level
scientific decision functions used by production handlers if doing so
would make it incapable of detecting implementation errors.

Shared low-level canonicalization/schema primitives are acceptable
where scientifically harmless.


======================================================================
62. IMPLEMENTATION TRACEABILITY
======================================================================

Create a complete machine-readable traceability table.

At minimum columns:

    contract_section
    normative_artifact
    normative_path_or_key
    scientific_operation
    implementation_package
    implementation_symbol
    test
    evidence_type
    status
    notes.

Require:

    normative scientific requirements without implementation = 0
    production scientific code without normative source = 0.


======================================================================
63. NO PLACEHOLDERS
======================================================================

Search production scientific paths for:

    TODO
    FIXME
    panic("not implemented")
    placeholder
    dummy
    mock scientific result
    transport-only scientific handler
    hard-coded PASS/FAIL

or equivalents.

Any reachable production scientific placeholder:

    IMPLEMENTATION_VALIDATION_FAILURE.


======================================================================
64. TRANSPORT IS NOT SCIENTIFIC IMPLEMENTATION
======================================================================

The existing execution/evidence transport infrastructure does not
satisfy scientific implementation requirements by itself.

The task must prove that the production executor invokes actual
scientific handlers for:

    fitting;
    M0–M5;
    PM;
    generation;
    F2;
    complexity;
    aggregation;
    gates.

A SHA-chain or evidence-transport test handler MUST NOT be accepted as
a production scientific handler.


======================================================================
65. EXECUTOR ARCHITECTURE
======================================================================

Integrate the scientific handlers with the existing distributed
execution architecture without changing scientific semantics.

The same scientific job must produce identical scientific evidence
whether executed:

    locally;
    remotely;
    after retry;
    after lease expiry;
    after coordinator restart.

Telemetry may differ.

Scientific evidence may not.


======================================================================
66. SCHEDULER BOUNDARY
======================================================================

The scheduler:

    leases;
    retries;
    transports;
    publishes.

The scheduler MUST NOT:

    choose scientific candidate;
    alter RNG seed;
    invent reachability;
    inspect verdict to redesign DAG;
    tune threshold;
    modify scientific payload.

Reachability is determined from frozen scientific status semantics,
not scheduler intuition.


======================================================================
67. DETERMINISTIC PUBLICATION
======================================================================

Preserve deterministic evidence publication:

    temporary artifact;
    schema validation;
    hash validation;
    closure validation;
    fsync;
    content-addressed final location;
    atomic JobID index.

Equal duplicate evidence:

    harmless telemetry event.

Conflicting evidence for same JobID:

    quarantine;
    fail closed;
    block dependent scientific acceptance.


======================================================================
68. CONFLICT HANDLING
======================================================================

Do not resolve conflicting same-JobID evidence using:

    first writer wins;
    last writer wins;
    majority;
    worker priority;
    timestamp.

Conflict is a run-integrity failure.


======================================================================
69. TEST LAYERS
======================================================================

Validation must include at least:

    unit tests;
    golden tests;
    property tests;
    differential tests;
    evidence-schema tests;
    evidence-only verifier tests;
    deterministic replay tests;
    bounded end-to-end tests;
    distributed execution tests.


======================================================================
70. INHERITED GOLDEN SUITES
======================================================================

Run all applicable inherited goldens from:

    Task85c;
    Task85c-b;
    Task85c-c;
    Task85c-e;
    Task85c-g;
    Task85c-i.

No inherited frozen regression may silently disappear.


======================================================================
71. GENERATION GOLDENS
======================================================================

Require full V1.2 generation golden suite:

    PASS.

Required permanent regression:

    PF_SC01 = CLOSED.

Require:

    generation paths = 26/26.


======================================================================
72. STATUS GOLDENS
======================================================================

Run the frozen status/reachability golden suite.

Require:

    PASS.

Explicitly test:

    FIT failure;
    numerical failure;
    induction cap;
    predictive FAIL;
    predictive NOT_ASSESSABLE;
    generation failure;
    protocol veto;
    NOT_REACHED propagation;
    multi-dependency suppression.


======================================================================
73. EVIDENCE GOLDENS
======================================================================

Run V1.2 evidence-schema positive and negative regressions inherited
from Task85c-i.

Require:

    H_SC01 = CLOSED
    mixed identity = FAIL_CLOSED
    unknown version = FAIL_CLOSED.


======================================================================
74. EXECUTION IDENTITY GOLDENS
======================================================================

Run E2 JobID regressions.

Require:

    H_SC02 = CLOSED
    R2_G01 = CLOSED
    R2_G02 = CLOSED
    EI01 = CLOSED.

Python/JavaScript reference equality from Task85c-i must remain valid;
the production implementation must agree with the frozen vectors.


======================================================================
75. MODEL GOLDENS
======================================================================

Create deterministic OPEN/disposable golden vectors for every
scientific candidate/route sufficient to verify:

    fitting;
    selected parameters;
    predictive values;
    fitted-model serialization;
    generation;
    complexity.

These goldens MUST NOT use blind/natural confirmatory outcomes.


======================================================================
76. PM GOLDENS
======================================================================

Create OPEN/disposable deterministic goldens for:

    PM1
    PM2
    PM4
    PM5
    PM6.

Include boundary cases:

    exact ties;
    quantile boundaries;
    insufficient PM6 coverage;
    PM6 saturation;
    Holm boundary;
    practical-floor boundary;
    empty/adaptive ECE-bin edge cases where applicable.


======================================================================
77. F2 GOLDENS
======================================================================

Create OPEN/disposable deterministic goldens for all 12 F2 metrics.

Include small artificial corpora where expected metric values can be
independently reconstructed.


======================================================================
78. COMPLEXITY GOLDENS
======================================================================

Create deterministic complexity goldens for each model family.

Require independent reconstruction of canonical bit costs.


======================================================================
79. AGGREGATION GOLDENS
======================================================================

Create evidence-only aggregation fixtures covering:

    adequate singleton;
    adequate equivalent multi-class component;
    all assessably inadequate;
    missing evidence;
    NOT_ASSESSABLE;
    procedure failure;
    protocol veto.

Require exact final verdicts.


======================================================================
80. TWO IMPLEMENTATIONS WHERE HIGH-RISK
======================================================================

Use independent reference implementations for scientifically
high-risk deterministic primitives where practical, at minimum:

    RNG/golden vectors;
    generation;
    JobID canonicalization;
    numerical aggregation/quantile boundary fixtures;
    representative PM calculations;
    representative F2 calculations;
    complexity fixtures.

The reference path must not simply wrap the production implementation.


======================================================================
81. SECOND-IMPLEMENTER CHALLENGE
======================================================================

Perform a second-implementer audit against the completed implementation.

Ask whether a competent independent implementer could make a different
scientifically consequential choice while still claiming conformance
to the frozen authorities.

Record every such candidate choice.

Success requires:

    SECOND_IMPLEMENTER_UNSTATED_CHOICES = 0.

If a real scientific ambiguity remains, do not choose silently.


======================================================================
82. SCIENTIFIC CONTRACT DEFECT POLICY
======================================================================

STOP with:

    TASK85C_H_SCIENTIFIC_CONTRACT_DEFECT

only if an unresolved authority ambiguity can change:

    fitted model;
    scientific RNG realization;
    generated corpus;
    PM/F2 value;
    threshold semantics;
    gate;
    adequacy;
    complexity/minimality;
    final verdict.

Do NOT recursively request scientific refreeze for harmless
implementation/provenance representation questions.


======================================================================
83. IMPLEMENTATION CHOICE POLICY
======================================================================

If the contract fully determines scientific behavior but not a
scientifically irrelevant software detail such as:

    package name;
    internal struct;
    function decomposition;
    cache layout;
    logging;
    temporary filename;

choose a deterministic engineering implementation, document it and
continue.

This is not a scientific contract defect.


======================================================================
84. SAME-CLASS IMPLEMENTATION DEFECTS
======================================================================

If multiple missing handlers or implementation bugs are discovered:

    register;
    fix;
    test;
    continue.

Do NOT stop merely because the existing executor is incomplete.

Completing the executor is the purpose of Task85c-h.


======================================================================
85. CRITICAL DISTINCTION FROM PREVIOUS TASK85C-H
======================================================================

The historical Task85c-h correctly stopped because normative
authorities contradicted one another.

That contradiction is now closed by Task85c-i.

The current Task85c-h MUST NOT stop simply because:

    fitting is not implemented;
    M0–M5 are not implemented;
    PM is not implemented;
    F2 is not implemented;
    production scientific dispatch is not implemented.

Those are implementation tasks to be completed here.

Only an actual unresolved scientific-contract ambiguity justifies
SCIENTIFIC_CONTRACT_DEFECT.


======================================================================
86. BOUNDED OPEN END-TO-END FIXTURE
======================================================================

After individual handlers pass, construct a bounded end-to-end
scientific fixture using only:

    OPEN controls;
    disposable artificial corpora;
    non-secret identifiers.

It must exercise the actual production code path:

    FIT
      →
    PREDICTIVE
      →
    GENERATION where reachable
      →
    F2_METRIC where reachable
      →
    COMPLEXITY
      →
    CANDIDATE_AGGREGATION
      →
    CONTROL_AGGREGATION.

Do not substitute mocks for scientific handlers.


======================================================================
87. BOUNDED FIXTURE SIZE
======================================================================

The bounded fixture may reduce:

    number of controls;
    number of candidate cells;
    corpus size

only where this is an explicitly non-production engineering fixture.

It MUST NOT change scientific semantics inside any executed job.

Record exactly which production dimensions are reduced.


======================================================================
88. REACHABILITY FIXTURES
======================================================================

The bounded validation set must include separate fixtures that force:

    fully reachable path;
    predictive FAIL;
    predictive NOT_ASSESSABLE;
    fit procedure failure;
    generation failure;
    protocol veto.

This validates actual status-driven dispatch.


======================================================================
89. LOCAL DETERMINISM
======================================================================

Repeat bounded fixtures locally.

Require byte-identical:

    fitted model artifacts;
    generated corpora;
    evidence;
    hashes;
    scientific statuses;
    final verdict.

Wall-clock/runtime telemetry is excluded.


======================================================================
90. DISTRIBUTED DETERMINISM
======================================================================

Run bounded OPEN fixtures through the real coordinator/worker
architecture.

Require scientific output byte-identical to local execution.

Use only non-secret/open fixtures.


======================================================================
91. RETRY DETERMINISM
======================================================================

For bounded OPEN jobs test:

    worker kill;
    lease expiry;
    retry;
    duplicate completion.

Require byte-identical accepted scientific evidence.


======================================================================
92. COORDINATOR RESTART
======================================================================

Restart the coordinator during a bounded OPEN run.

After resume:

    scientific evidence closure

must equal uninterrupted execution.


======================================================================
93. CONFLICT INJECTION
======================================================================

Inject deliberately conflicting evidence for the same disposable
JobID.

Require:

    conflict detected;
    quarantine;
    dependent acceptance blocked;
    fail closed.


======================================================================
94. EVIDENCE-ONLY RECONSTRUCTION
======================================================================

For bounded end-to-end fixtures, independently reconstruct the final
scientific result from frozen evidence only.

Require equality with production aggregation output.


======================================================================
95. RACE TESTING
======================================================================

Run applicable Go tests with:

    -race

for scientific/executor packages where supported.

No data race capable of changing scientific output is acceptable.


======================================================================
96. ARCHITECTURE/PLATFORM
======================================================================

Production target remains:

    Linux/amd64.

Task85c-h may validate on development systems additionally, but
scientific implementation must be portable to the production target
without semantic changes.


======================================================================
97. BUILD REPRODUCIBILITY
======================================================================

Record:

    source-tree identity;
    git commit if available;
    dirty state;
    Go version;
    GOOS;
    GOARCH;
    build flags;
    module dependency identity.

A final production binary freeze is NOT required here; that belongs to
Task86C-v2-production-freeze.

A validation binary may be built and hashed.


======================================================================
98. PERFORMANCE CHECK
======================================================================

Perform bounded engineering benchmarks sufficient to detect obvious
non-termination or catastrophic implementation behavior.

This is NOT the final production capacity qualification.

Final capacity projection belongs to:

    Task86C-v2-production-freeze.

Do not simplify scientific algorithms for performance.


======================================================================
99. HIGH-RISK PERFORMANCE PATHS
======================================================================

At minimum exercise:

    M3 exact;
    M3 approximate;
    M4 fitting;
    PM2 resampling;
    PM5 resampling;
    PM6 resampling/permutation;
    generation;
    F2 metrics.

If a frozen scientific bound is reached, emit the frozen status rather
than changing the algorithm.


======================================================================
100. NO THRESHOLD CALIBRATION
======================================================================

Do NOT compute/freeze production DEVELOPMENT thresholds in Task85c-h.

OPEN/disposable threshold calculations may be used solely to test the
implementation.

They must be clearly marked:

    NON_PRODUCTION_TEST_FIXTURE.


======================================================================
101. NO PRODUCTION CONTROL MATERIALIZATION
======================================================================

Require at task completion:

    PRODUCTION_OPEN_CONTROLS_MATERIALIZED = 0
    PRODUCTION_BLIND_CONTROLS_MATERIALIZED = 0
    PRODUCTION_NATURAL_CONTROLS_MATERIALIZED = 0.

Task86C-v2-production-freeze will materialize the concrete production
run in the frozen order.


======================================================================
102. NO ESCROW
======================================================================

Require:

    PRODUCTION_ESCROW_KEY_CREATED = NO.

Disposable non-secret test identifiers are allowed.

Do not create a production-like secret escrow merely to test code.


======================================================================
103. NO PRODUCTION JOBIDS
======================================================================

E2 JobID implementation must be tested with disposable fixtures.

Require:

    PRODUCTION_JOBIDS_CREATED = 0.


======================================================================
104. NO PRODUCTION DAG
======================================================================

The generic DAG semantics may be implemented and tested.

Do NOT instantiate the concrete 1,321,152-job production DAG.

Require:

    PRODUCTION_DAG_CREATED = NO.


======================================================================
105. EXPECTED FUTURE PRODUCTION DAG
======================================================================

Implementation must nevertheless support the already frozen future
production graph:

    candidate fits:
        8,256

    predictive:
        8,256

    generation:
        99,072

    F2 metric:
        1,188,864

    complexity:
        8,256

    candidate aggregation:
        8,256

    control aggregation:
        192

    total jobs:
        1,321,152

    dependency edges:
        2,617,152

    terminal cells:
        8,256.

These cardinalities MUST NOT be changed by Task85c-h.


======================================================================
106. NOT_REACHED CARDINALITY
======================================================================

Future production DAG cardinality is independent of outcomes.

Jobs suppressed by reachability still exist conceptually and produce
the required NOT_REACHED evidence/status representation.

Do not implement dynamic graph deletion that changes frozen
cardinality.


======================================================================
107. SCIENTIFIC FIREWALL
======================================================================

Task85c-h MUST NOT access or derive:

    production blind truth;
    production blind recovery outcomes;
    natural-language confirmatory recovery outcomes;
    Voynich scientific outcomes.

Require:

    SCIENTIFIC_FIREWALL = INTACT.


======================================================================
108. VOYNICH FIREWALL
======================================================================

Preserve:

    voynich_paths_forbidden = true

where frozen.

Do not use the Voynich corpus to test whether implementation "looks
right".

Implementation validation must use open/disposable fixtures only.


======================================================================
109. NATURAL-LANGUAGE FIREWALL
======================================================================

Do not run the 36 natural-language confirmatory controls.

Frozen source/preprocessing contracts may be parsed/validated without
computing confirmatory recovery results if needed for static
implementation validation.

Prefer no confirmatory corpus access unless technically necessary.


======================================================================
110. BLIND FIREWALL
======================================================================

Do not create or inspect the 144 production blind synthetic controls.

Synthetic OPEN/disposable controls used for implementation tests must
be unmistakably outside the production blind registry.


======================================================================
111. REQUIRED IMPLEMENTATION PACKAGES
======================================================================

Package organization is an engineering choice, but the codebase must
provide clear production implementations for at least:

    model fitting;
    M0;
    M1;
    M2;
    M3;
    M4;
    M5;
    predictive metrics;
    generation;
    F2 metrics;
    complexity;
    status/reachability;
    aggregation;
    evidence production;
    evidence verification;
    JobID/E2;
    scientific dispatch.

Do not require these exact package names if existing repository
structure provides a cleaner deterministic mapping.


======================================================================
112. SCIENTIFIC DISPATCH REGISTRY
======================================================================

Create a machine-checkable registry mapping frozen scientific job
types/candidates/routes to production handlers.

Require:

    missing handler mappings = 0
    duplicate ambiguous mappings = 0
    placeholder mappings = 0.


======================================================================
113. STAGE HANDLER COVERAGE
======================================================================

Require production scientific support for all seven stages:

    FIT
    PREDICTIVE
    GENERATION
    F2_METRIC
    COMPLEXITY
    CANDIDATE_AGGREGATION
    CONTROL_AGGREGATION.

Report:

    PRODUCTION_STAGES_IMPLEMENTED = 7/7.


======================================================================
114. ROUTE COVERAGE
======================================================================

Explicitly report M3 route coverage:

    M3_EXACT_IMPLEMENTED =
        YES

    M3_APPROX_IMPLEMENTED =
        YES.


======================================================================
115. GENERATION PATH COVERAGE
======================================================================

Report:

    GENERATION_PATHS_IMPLEMENTED =
        26/26.

Do not confuse generation semantic reference tests with actual
production dispatch coverage.


======================================================================
116. PM COVERAGE
======================================================================

Report individually:

    PM1_IMPLEMENTED
    PM2_IMPLEMENTED
    PM4_IMPLEMENTED
    PM5_IMPLEMENTED
    PM6_IMPLEMENTED.

All required values:

    YES.


======================================================================
117. F2 COVERAGE
======================================================================

Report:

    F2_METRICS_IMPLEMENTED =
        12/12.


======================================================================
118. EVIDENCE COVERAGE
======================================================================

Report:

    V1_2_EVIDENCE_TYPES_IMPLEMENTED =
        15/15.

Every actual production handler output type must have a V1.2 schema
mapping.


======================================================================
119. STATUS COVERAGE
======================================================================

Report:

    STATUSES_IMPLEMENTED =
        13/13

    DAG_STAGES_IMPLEMENTED =
        7/7

    DIRECT_TRANSITIONS_IMPLEMENTED =
        45/45.


======================================================================
120. TRACEABILITY GATE
======================================================================

Before success require:

    scientific requirements unmapped to code = 0
    production scientific handlers unmapped to authority = 0
    production scientific handlers without tests = 0
    production scientific handlers without evidence mapping = 0.


======================================================================
121. REQUIRED DIRECTORY
======================================================================

Use:

    research/phase3/task85c-h/

for the successful retry.

Preserve the previous failed attempt separately before writing new
results.

If repository history already guarantees immutable preservation, the
exact archive location is an engineering choice.


======================================================================
122. REQUIRED ARTIFACTS
======================================================================

At minimum produce:

    TASK85C_H_DESIGN.md
    TASK85C_H_REPORT.md

    TASK85C_H_AUTHORITY_VALIDATION.tsv
    TASK85C_H_IMPLEMENTATION_TRACEABILITY.tsv
    TASK85C_H_HANDLER_REGISTRY.json
    TASK85C_H_STAGE_COVERAGE.tsv
    TASK85C_H_CANDIDATE_COVERAGE.tsv
    TASK85C_H_GENERATION_COVERAGE.tsv
    TASK85C_H_PM_COVERAGE.tsv
    TASK85C_H_F2_COVERAGE.tsv
    TASK85C_H_EVIDENCE_COVERAGE.tsv
    TASK85C_H_STATUS_REACHABILITY_COVERAGE.tsv

    TASK85C_H_GOLDEN_VALIDATION.tsv
    TASK85C_H_MODEL_VALIDATION.tsv
    TASK85C_H_PM_VALIDATION.tsv
    TASK85C_H_F2_VALIDATION.tsv
    TASK85C_H_COMPLEXITY_VALIDATION.tsv
    TASK85C_H_AGGREGATION_VALIDATION.tsv
    TASK85C_H_EVIDENCE_VALIDATION.tsv
    TASK85C_H_JOBID_VALIDATION.tsv

    TASK85C_H_LOCAL_DETERMINISM.tsv
    TASK85C_H_DISTRIBUTED_DETERMINISM.tsv
    TASK85C_H_FAILURE_INJECTION.tsv
    TASK85C_H_SECOND_IMPLEMENTER_AUDIT.tsv

    TASK85C_H_VALIDATION.tsv
    TASK85C_H_RESULTS_MANIFEST.json

    reference/
        verify_authority_closure.*
        verify_handler_registry.*
        verify_traceability.*
        verify_generation_goldens.*
        verify_status_reachability.*
        verify_evidence_v1_2.*
        verify_jobid_e2.*
        verify_model_goldens.*
        verify_pm_goldens.*
        verify_f2_goldens.*
        verify_complexity_goldens.*
        verify_aggregation_goldens.*
        verify_evidence_only_reconstruction.*
        verify_task85c_h_manifest.*

and exactly one terminal marker.


======================================================================
123. SOURCE IMPLEMENTATION ARTIFACTS
======================================================================

Scientific production code may reside in normal repository source
locations rather than under:

    research/phase3/task85c-h/.

The results manifest must hash-bind all scientific source files that
form the validated implementation closure or bind an unambiguous
source-tree identity sufficient to reconstruct that closure.

Do not make the research directory the only provenance for code that
actually lives elsewhere.


======================================================================
124. IMPLEMENTATION CLOSURE ROOT
======================================================================

Compute an immutable:

    SCIENTIFIC_IMPLEMENTATION_ROOT_SHA256 =
        <sha256>.

It must bind all production scientific implementation sources and
normative handler registries necessary for the validated executor.

Define the root algorithm explicitly.


======================================================================
125. VALIDATION ROOT
======================================================================

Compute an immutable validation root binding:

    golden results;
    traceability;
    coverage;
    deterministic tests;
    distributed tests;
    failure-injection tests;
    evidence-only verification.

Report:

    VALIDATION_ROOT_SHA256 =
        <sha256>.


======================================================================
126. TASK ARTIFACT ROOT
======================================================================

Compute:

    TASK85C_H_ARTIFACT_ROOT_SHA256 =
        <sha256>

over the task artifacts using an explicit deterministic root
definition and manifest exclusion rule.


======================================================================
127. REQUIRED PARENT HASHES IN RESULTS MANIFEST
======================================================================

The results manifest must bind at minimum:

    V1.2 Markdown SHA
    V1.2 machine SHA
    I1 machine SHA
    E2 machine SHA
    V1.2 evidence schema root
    V1.2 evidence registry SHA
    status/reachability V2 SHA
    generation semantics SHA
    generation state-machine SHA
    generation golden root
    generation serialization SHA
    Task85c-i artifact root
    Task85c-i results manifest SHA.


======================================================================
128. REQUIRED IMPLEMENTATION VERDICTS
======================================================================

Report at minimum:

    CANDIDATES_IMPLEMENTED =
        <count>/43

    M0_IMPLEMENTED =
        YES/NO

    M1_IMPLEMENTED =
        YES/NO

    M2_IMPLEMENTED =
        YES/NO

    M3_EXACT_IMPLEMENTED =
        YES/NO

    M3_APPROX_IMPLEMENTED =
        YES/NO

    M4_IMPLEMENTED =
        YES/NO

    M5_IMPLEMENTED =
        YES/NO

    PM1_IMPLEMENTED =
        YES/NO

    PM2_IMPLEMENTED =
        YES/NO

    PM4_IMPLEMENTED =
        YES/NO

    PM5_IMPLEMENTED =
        YES/NO

    PM6_IMPLEMENTED =
        YES/NO

    GENERATION_PATHS_IMPLEMENTED =
        <count>/26

    F2_METRICS_IMPLEMENTED =
        <count>/12

    V1_2_EVIDENCE_TYPES_IMPLEMENTED =
        <count>/15

    PRODUCTION_STAGES_IMPLEMENTED =
        <count>/7

    STATUSES_IMPLEMENTED =
        <count>/13

    DIRECT_TRANSITIONS_IMPLEMENTED =
        <count>/45.


======================================================================
129. REQUIRED VALIDATION VERDICTS
======================================================================

Report at minimum:

    AUTHORITY_GATE =
        PASS/FAIL

    TRACEABILITY =
        PASS/FAIL

    MODEL_GOLDENS =
        PASS/FAIL

    GENERATION_GOLDENS =
        PASS/FAIL

    PM_GOLDENS =
        PASS/FAIL

    F2_GOLDENS =
        PASS/FAIL

    COMPLEXITY_GOLDENS =
        PASS/FAIL

    AGGREGATION_GOLDENS =
        PASS/FAIL

    EVIDENCE_V1_2_VALIDATION =
        PASS/FAIL

    EVIDENCE_ONLY_RECONSTRUCTION =
        PASS/FAIL

    STATUS_REACHABILITY_VALIDATION =
        PASS/FAIL

    JOBID_E2_VALIDATION =
        PASS/FAIL

    LOCAL_DETERMINISM =
        PASS/FAIL

    DISTRIBUTED_DETERMINISM =
        PASS/FAIL

    RETRY_DETERMINISM =
        PASS/FAIL

    COORDINATOR_RESTART =
        PASS/FAIL

    CONFLICT_INJECTION =
        PASS/FAIL

    SECOND_IMPLEMENTER_UNSTATED_CHOICES =
        <count>.


======================================================================
130. REQUIRED REGRESSION VERDICTS
======================================================================

Report:

    H_SC01 =
        CLOSED/OPEN

    H_SC02 =
        CLOSED/OPEN

    R2_G01 =
        CLOSED/OPEN

    R2_G02 =
        CLOSED/OPEN

    EI01 =
        CLOSED/OPEN

    PF_SC01 =
        CLOSED/OPEN

    GENERATION_PATHS =
        <count>/26.


======================================================================
131. REQUIRED FIREWALL/MATERIALIZATION VERDICTS
======================================================================

Report:

    SCIENTIFIC_SEMANTIC_CHANGE =
        <count>

    SCIENTIFIC_FIREWALL =
        INTACT/VIOLATED

    PRODUCTION_ESCROW_KEY_CREATED =
        YES/NO

    PRODUCTION_OPEN_CONTROLS_MATERIALIZED =
        <count>

    PRODUCTION_BLIND_CONTROLS_MATERIALIZED =
        <count>

    PRODUCTION_NATURAL_CONTROLS_MATERIALIZED =
        <count>

    PRODUCTION_JOBIDS_CREATED =
        <count>

    PRODUCTION_DAG_CREATED =
        YES/NO

    VOYNICH_EVALUATED =
        YES/NO

    CONFIRMATORY_OUTCOMES_INSPECTED =
        YES/NO.


======================================================================
132. SUCCESS GATE
======================================================================

TASK86C_V2_PRODUCTION_FREEZE_READY = SUPPORTED only if ALL hold:

1. Authority gate PASS.
2. V1.2 contract hashes match.
3. I1 hash matches.
4. E2 hash matches.
5. V1.2 evidence schema root matches.
6. Status/reachability V2 hash matches.
7. Generation semantics hash matches.
8. Generation golden root matches.
9. Task85c-i artifact root matches.
10. Task85c-i manifest matches.
11. H-SC01 remains CLOSED.
12. H-SC02 remains CLOSED.
13. R2-G01 remains CLOSED.
14. R2-G02 remains CLOSED.
15. EI01 remains CLOSED.
16. PF-SC01 remains CLOSED.
17. All 43 candidates are implemented.
18. M0 implemented.
19. M1 implemented.
20. M2 implemented.
21. M3 exact implemented.
22. M3 approximate implemented.
23. M4 implemented.
24. M5 implemented.
25. PM1 implemented.
26. PM2 implemented.
27. PM4 implemented.
28. PM5 implemented.
29. PM6 implemented.
30. Generation production paths implemented 26/26.
31. F2 metrics implemented 12/12.
32. V1.2 evidence types implemented 15/15.
33. Production stages implemented 7/7.
34. Statuses implemented 13/13.
35. Direct transitions implemented 45/45.
36. Traceability PASS.
37. Unmapped scientific requirements = 0.
38. Unmapped production scientific code = 0.
39. Production handlers without tests = 0.
40. Production handlers without evidence mapping = 0.
41. Model goldens PASS.
42. Generation goldens PASS.
43. PM goldens PASS.
44. F2 goldens PASS.
45. Complexity goldens PASS.
46. Aggregation goldens PASS.
47. V1.2 evidence validation PASS.
48. Evidence-only reconstruction PASS.
49. Status/reachability validation PASS.
50. E2 JobID validation PASS.
51. Local determinism PASS.
52. Distributed determinism PASS.
53. Retry determinism PASS.
54. Coordinator restart PASS.
55. Conflict injection PASS.
56. Bounded OPEN end-to-end production-handler fixture PASS.
57. Second-implementer unstated scientific choices = 0.
58. Scientific semantic change = 0.
59. Scientific firewall INTACT.
60. Production escrow key created = NO.
61. Production open controls materialized = 0.
62. Production blind controls materialized = 0.
63. Production natural controls materialized = 0.
64. Production JobIDs created = 0.
65. Production DAG created = NO.
66. Voynich evaluated = NO.
67. Confirmatory outcomes inspected = NO.
68. Scientific implementation root frozen.
69. Validation root frozen.
70. Task artifact root frozen.
71. Results-manifest closure PASS.


======================================================================
133. STOP — AUTHORITY FAILURE
======================================================================

STOP:

    TASK85C_H_AUTHORITY_GATE_FAILED

if the frozen V1.2/I1/E2/evidence/status/generation authority closure
cannot be verified before implementation.


======================================================================
134. STOP — SCIENTIFIC CONTRACT DEFECT
======================================================================

STOP:

    TASK85C_H_SCIENTIFIC_CONTRACT_DEFECT

only if implementation exposes a genuinely unresolved scientific
ambiguity capable of changing:

    scientific data;
    fitted model;
    RNG realization;
    generated corpus;
    PM/F2;
    threshold semantics;
    gate;
    adequacy;
    minimality;
    final verdict.

The report must include the smallest reproducer and competing
scientifically valid interpretations.


======================================================================
135. STOP — IMPLEMENTATION VALIDATION FAILURE
======================================================================

STOP:

    TASK85C_H_IMPLEMENTATION_VALIDATION_FAILURE

if the scientific contract is sufficiently specified but the
implementation cannot be made to satisfy it.

Examples:

    handler remains missing;
    golden mismatch;
    wrong evidence;
    numerical implementation mismatch;
    incomplete candidate coverage;
    incomplete F2 coverage;
    incomplete PM coverage;
    traceability gap;
    evidence-only reconstruction mismatch.

This is NOT a reason to change the scientific contract.


======================================================================
136. STOP — DETERMINISM FAILURE
======================================================================

STOP:

    TASK85C_H_DETERMINISM_FAILURE

if equivalent executions can produce scientifically different bytes,
statuses, metrics, models, generated corpora or evidence.


======================================================================
137. STOP — DISTRIBUTED EXECUTION FAILURE
======================================================================

STOP:

    TASK85C_H_DISTRIBUTED_EXECUTION_FAILURE

if local scientific execution is correct but integration with the
real coordinator/worker architecture cannot preserve frozen scientific
identity/evidence semantics.


======================================================================
138. STOP — EVIDENCE INTEGRITY FAILURE
======================================================================

STOP:

    TASK85C_H_EVIDENCE_INTEGRITY_FAILURE

if handler-produced evidence cannot satisfy the frozen V1.2 evidence
schemas or evidence-only reconstruction cannot validate the scientific
result.


======================================================================
139. STOP — FIREWALL
======================================================================

STOP:

    TASK85C_H_SCIENTIFIC_FIREWALL_VIOLATION

if prohibited production/confirmatory/Voynich material is created,
accessed or used.


======================================================================
140. NO STOP FOR ORDINARY IMPLEMENTATION WORK
======================================================================

Do NOT STOP merely because initial repository inspection finds:

    no M0–M5 implementation;
    no fitting;
    no PM;
    no F2;
    no scientific generation dispatch;
    no scientific aggregation.

Implement them.

This is the central difference between this task and the failed
pre-I1 Task85c-h attempt.


======================================================================
141. SUCCESS TERMINAL MARKER
======================================================================

On success emit exactly:

    G1V2_V1_2_PRODUCTION_SCIENTIFIC_IMPLEMENTATION_H_FROZEN


======================================================================
142. FAILURE TERMINAL MARKERS
======================================================================

Exactly one of:

    TASK85C_H_AUTHORITY_GATE_FAILED

    TASK85C_H_SCIENTIFIC_CONTRACT_DEFECT

    TASK85C_H_IMPLEMENTATION_VALIDATION_FAILURE

    TASK85C_H_DETERMINISM_FAILURE

    TASK85C_H_DISTRIBUTED_EXECUTION_FAILURE

    TASK85C_H_EVIDENCE_INTEGRITY_FAILURE

    TASK85C_H_SCIENTIFIC_FIREWALL_VIOLATION.


======================================================================
143. SUCCESSFUL FINAL OUTPUT
======================================================================

On success print at minimum:

    SCIENTIFIC_CONTRACT_VERSION =
        G1_V2_EXECUTABLE_CONTRACT_V1_2

    SCIENTIFIC_CONTRACT_MACHINE_SHA256 =
        29e39e0c25dc8033f784480fdc537e3ede9eeb69baa0607c9f249d796d6b42dc

    INTEGRATION_AUTHORITY =
        G1V2_V1_2_INTEGRATION_SUPPLEMENT_I1

    I1_MACHINE_SHA256 =
        35ecf0bfc9a9c27bb63d33b074bc399dd9256692620ee06a54e592e0d1e867b2

    EXECUTION_IDENTITY_AUTHORITY =
        G1V2_EXECUTION_IDENTITY_ERRATUM_E2

    E2_MACHINE_SHA256 =
        cfaa8c1cb787380baca5a391d077e5d2b855df8bba767ad5b601457e06eb0070

    EVIDENCE_CONTRACT =
        G1V2_EVIDENCE_CONTRACT_V1_2

    EVIDENCE_SCHEMA_ROOT_SHA256 =
        39c4c3bee96ee58ddd38552cbb16fdc2f994a390b86a6645e1420c3cb67eca81

    CANDIDATES_IMPLEMENTED =
        43/43

    PRODUCTION_STAGES_IMPLEMENTED =
        7/7

    STATUSES_IMPLEMENTED =
        13/13

    DIRECT_TRANSITIONS_IMPLEMENTED =
        45/45

    GENERATION_PATHS_IMPLEMENTED =
        26/26

    F2_METRICS_IMPLEMENTED =
        12/12

    V1_2_EVIDENCE_TYPES_IMPLEMENTED =
        15/15

    PM1_IMPLEMENTED =
        YES

    PM2_IMPLEMENTED =
        YES

    PM4_IMPLEMENTED =
        YES

    PM5_IMPLEMENTED =
        YES

    PM6_IMPLEMENTED =
        YES

    H_SC01 =
        CLOSED

    H_SC02 =
        CLOSED

    R2_G01 =
        CLOSED

    R2_G02 =
        CLOSED

    EI01 =
        CLOSED

    PF_SC01 =
        CLOSED

    GENERATION_PATHS =
        26/26

    TRACEABILITY =
        PASS

    MODEL_GOLDENS =
        PASS

    GENERATION_GOLDENS =
        PASS

    PM_GOLDENS =
        PASS

    F2_GOLDENS =
        PASS

    COMPLEXITY_GOLDENS =
        PASS

    AGGREGATION_GOLDENS =
        PASS

    EVIDENCE_V1_2_VALIDATION =
        PASS

    EVIDENCE_ONLY_RECONSTRUCTION =
        PASS

    STATUS_REACHABILITY_VALIDATION =
        PASS

    JOBID_E2_VALIDATION =
        PASS

    LOCAL_DETERMINISM =
        PASS

    DISTRIBUTED_DETERMINISM =
        PASS

    RETRY_DETERMINISM =
        PASS

    COORDINATOR_RESTART =
        PASS

    CONFLICT_INJECTION =
        PASS

    SECOND_IMPLEMENTER_UNSTATED_CHOICES =
        0

    SCIENTIFIC_SEMANTIC_CHANGE =
        0

    SCIENTIFIC_FIREWALL =
        INTACT

    PRODUCTION_ESCROW_KEY_CREATED =
        NO

    PRODUCTION_OPEN_CONTROLS_MATERIALIZED =
        0

    PRODUCTION_BLIND_CONTROLS_MATERIALIZED =
        0

    PRODUCTION_NATURAL_CONTROLS_MATERIALIZED =
        0

    PRODUCTION_JOBIDS_CREATED =
        0

    PRODUCTION_DAG_CREATED =
        NO

    VOYNICH_EVALUATED =
        NO

    CONFIRMATORY_OUTCOMES_INSPECTED =
        NO

    SCIENTIFIC_IMPLEMENTATION_ROOT_SHA256 =
        <sha256>

    VALIDATION_ROOT_SHA256 =
        <sha256>

    TASK85C_H_ARTIFACT_ROOT_SHA256 =
        <sha256>

    TASK86C_V2_PRODUCTION_FREEZE_READY =
        SUPPORTED

    NEXT_TASK =
        Task86C-v2-production-freeze

    TERMINAL_MARKER =
        G1V2_V1_2_PRODUCTION_SCIENTIFIC_IMPLEMENTATION_H_FROZEN.


======================================================================
144. FAILURE OUTPUT
======================================================================

On failure print at minimum:

    SCIENTIFIC_CONTRACT_VERSION =
        G1_V2_EXECUTABLE_CONTRACT_V1_2

    FAILURE_CLASS =
        <exact failure class>

    BLOCKING_FINDINGS =
        <finding IDs>

    CANDIDATES_IMPLEMENTED =
        <count>/43

    PRODUCTION_STAGES_IMPLEMENTED =
        <count>/7

    GENERATION_PATHS_IMPLEMENTED =
        <count>/26

    F2_METRICS_IMPLEMENTED =
        <count>/12

    SCIENTIFIC_SEMANTIC_CHANGE =
        <count>

    SCIENTIFIC_FIREWALL =
        <state>

    PRODUCTION_MATERIALIZATION_OCCURRED =
        YES/NO

    TASK86C_V2_PRODUCTION_FREEZE_READY =
        NOT_SUPPORTED

    TERMINAL_MARKER =
        <exact terminal marker>.


======================================================================
145. DEFINITION OF DONE
======================================================================

Task85c-h is complete when the repository contains an actual production
scientific implementation of the already frozen G1-v2 V1.2 protocol,
rather than only distributed execution/evidence transport.

The implementation must cover:

    all 43 candidates;
    M0–M5;
    M3 exact and approximate routes;
    fitting;
    PM1/PM2/PM4/PM5/PM6;
    all 26 generation paths;
    all 12 F2 metrics;
    structural evaluation;
    complexity;
    equivalence;
    minimality;
    all seven production stages;
    all frozen statuses/reachability;
    aggregation;
    final verdict;
    V1.2 evidence;
    E2 JobID identity.

The implementation must be:

    traceable to frozen authority;
    deterministic;
    independently testable;
    evidence-verifiable;
    distributed-execution compatible;
    free of scientifically consequential unstated choices.

No production experiment has yet been materialized.

No production thresholds have yet been frozen.

No production blind controls or escrow have yet been created.

No natural confirmatory outcomes have been evaluated.

No Voynich data have been evaluated.

Successful completion authorizes only the next preparation step:

    Task86C-v2-production-freeze.

It does NOT authorize:

    Task86C-v2-run

directly.
